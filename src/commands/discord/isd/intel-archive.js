// delete an intelligence file and stores it somewhere else
// requires: user that the file is associated with (ID)
// requires: be SC-4+ or ISD
// delete from MongoDB (active server) and store in Github (archive server)